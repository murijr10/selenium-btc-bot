# selenium-btc-bot
This project uses Selenium to scrape the real-time Bitcoin price from Preev.com. The script compares the current price against a predefined threshold and automatically sends a notification to a Discord channel via Webhooks if the price exceeds that limit.
